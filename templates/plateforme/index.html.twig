{% extends 'base.html.twig' %}

{% block title %}Plateforme index{% endblock %}

{% block body %}
    <h1>Plateforme index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Logo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for plateforme in plateformes %}
            <tr>
                <td>{{ plateforme.id }}</td>
                <td>{{ plateforme.titre }}</td>
                <td>{{ plateforme.logo }}</td>
                <td>
                    <a href="{{ path('plateforme_show', {'id': plateforme.id}) }}">show</a>
                    <a href="{{ path('plateforme_edit', {'id': plateforme.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('plateforme_new') }}">Create new</a>
{% endblock %}

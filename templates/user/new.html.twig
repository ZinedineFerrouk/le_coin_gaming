{% extends 'admin.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center">Créer un utilisateur</h1>

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('user_index') }}" class="btn btn-primary my-2">Retour à la liste</a>
</div>
{% endblock %}
